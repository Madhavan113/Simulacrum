{
  "buildCommand": "pnpm --filter @simulacrum/types run build && pnpm --filter @simulacrum/ui run build",
  "outputDirectory": "packages/ui/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
